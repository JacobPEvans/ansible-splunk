---
# README validator configuration for ansible-splunk
# All READMEs in this repo are required to have Installation and Usage sections.
# Note: inline [] is not supported by the simple yaml parser used here; use
# list syntax instead.
required_sections:
  - Installation
  - Usage
optional_sections:
  - Contributing
  - License
